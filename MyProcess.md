## 2025年5月版本：

1.**起因**：本地通过DeepSeek手搓了一个简易版本的SDS，那时候我还不知道API是什么，一切知识来自于测试反馈。我就在本地windows电脑里面测试与学习。我尝试找DeepSeek的原对话，但是好像找不到了。只留有后面想离线云端的升级版本思路。

本地能跑之后，想搬到一台有 GPU 的服务器上，再让 iOS 调用它。我知道有个叫 Docker 的东西，就像打包一个野餐包——本地装好的东西全塞进去，到那边打开就能用。

**第一次被岔开**：要先写个 Dockerfile，我不会写，让它生成。生出来一个十几 G 的包，我觉得太大，才知道还有个 `.dockerignore`。

**然后卡在打包上**：`docker build` 一直失败。原因不是代码，是国内网络拉不下来东西。阿里云加速器、清华源、中科大源——一个环节一个环节地通。

**打包成功，上传又不行**：我本来以为用 `scp` 就够了。但 SSH 能连、`scp` 不能用。**这台服务器不是我以为的那种服务器**，它不给你开端口，传文件得先走 OSS 中转。上传的时候我还往内网 IP 上推，ping 了半天不通，才反应过来要用公网 IP。

**要命的一步**：镜像传上去了，**服务器不支持 Docker**。整条打包链路白做一遍。只能从头手搭环境：下 Python 源码、编译、建虚拟环境、装依赖、下模型。模型下载还被墙，又换成华为云源。

**这些坑挨个填完之后**，才轮到真正的目标——把 iOS 和服务端连起来。

**我当时的想法**：两边都跑起来了，那我改一下 iOS 里的地址，指到服务器的公网 IP，应该就通了。

**结果不通**。iOS 报一个 ATS 的错。我以为是我没配好 iOS，就去改 `Info.plist`：加例外、清 Xcode 缓存、重启、换写法……**改了五轮，一模一样的错**。

**直到我停下来想了一下整件事**：我一直盯着 iOS 那头，因为报错在那边。但我用 `netstat` 查的时候，服务端**根本没有在监听**。

**然后我想起了一件我早就知道的事**：这台服务器 SSH 能连、`scp` 不行、传文件要走 OSS、控制台里我**从来没见过"安全组"这一项**。

**那一刻就明白了**：不是 iOS 配错了，也不是我代码写错了。是**这台机器根本不允许外面连进来**。

**于是停在这里**。服务器也快到期了。没改成，那时候还不知道哪里出现了错误。

---

## 2026年3月版本

2.主要是我按照前后端分离的原则分开了，因为那时候我用的是windows的wsl2来开发，但是实际测试在windows上，因此，失败的原因好似因为硬件的原因：

按我的回忆，当时放弃继续部署，主要是因为麦克风采集链路在 Windows + WSL2 环境下不容易接通。那时 AI 给出的判断是：如果要在 Windows 原生环境重新编译运行，或者继续通过 WSL2 让 Linux 程序访问 Windows 麦克风，都需要额外处理依赖、音频设备和容器配置，工作量不小。这里记录的是当时的回忆；目前没有保留当时的报错或设备列表，无法还原并确认具体失败步骤。

**当前代码能确认的部分**
    - `utils/Speech_input.py` 在 Python 进程中通过 `sounddevice.InputStream` 打开默认输入设备采音，然后才把录到的音频交给 faster-whisper 转写。因此，麦克风设备不可见时，问题发生在 Whisper 识别之前。
    - `docker-compose.yml` 将宿主机的 `/dev/snd` 映射到容器。这是面向 Linux ALSA 声卡的配置，并不等同于把 Windows 麦克风自动传入 WSL2 或 Docker 容器。
    - `Dockerfile` 使用 Jetson Orin / JetPack 的 ARM64 Linux 基础镜像，并安装 Linux 音频依赖；项目当前的 Docker 配置不是 Windows 原生运行配置。

这些实现与“Windows + WSL2 下容器看不到麦克风”的回忆相吻合，但不能单凭当前代码证明它就是当时唯一的失败原因。

**为什么重新尝试会比较麻烦**

    1. **WSL2 的设备边界**：Linux 进程和容器需要能访问录音输入设备。Windows 上的麦克风不会因为启用了 WSL2 就自动成为容器里的 `/dev/snd` 设备。若采用 WSLg 的音频转发方式，还需要确认麦克风输入是否可用，并配置 PulseAudio socket、容器挂载和访问权限；现有 Compose 默认没有这样配置。
    2. **容器和音频后端**：即使容器能启动，也要让 PortAudio（`sounddevice` 使用的底层库）找到可用的输入设备，并验证采样率、声道等参数。设备枚举为空和 Whisper 模型无法加载是不同层次的问题。
    3. **Windows 原生运行不是直接重用当前镜像**：当前 Dockerfile 面向 Jetson Linux ARM64，语音识别还配置为 CUDA，并依赖特定平台的 CTranslate2 wheel。改成 Windows 原生运行，需要重新核对 Python、PortAudio、faster-whisper / CTranslate2 和 CUDA 的兼容组合；当前的 Jetson 镜像及 wheel 不能直接当作 Windows 构建使用。

**当时结论**

继续尝试并非绝对做不到，而是需要单独打通麦克风的跨系统/容器访问，并可能为 Windows 重建一套依赖和运行配置。对于当时的项目状态，这很容易从“部署应用”扩展成“移植音频采集与推理运行环境”，因此选择暂时放弃是可以理解的。

若以后重试，优先考虑在音频设备可直接访问的 Linux 主机上运行；若仍使用 Windows + WSL2，则先单独确认容器能枚举并录到麦克风，再启动完整应用。另一条路线是把采音放到 Windows 浏览器或原生前端，再把音频发送给后端处理，避免让 Linux 容器直接访问 Windows 麦克风。

---

## 2026年9月版本

3.成功在Linux真机上实现SDS的系统构建，且用上了NVIDIA的GPU用于加速，成功完成一年前的想法。在成功实现SDS后，还顺便实现了一些工业控制PLC。
